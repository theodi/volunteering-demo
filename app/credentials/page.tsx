import { UserCredentials } from "@/app/components/credentials/UserCredentials";

export default function CredentialsPage() {
  return (
    <main className="mx-auto w-full px-4 py-6 sm:px-6 sm:py-8">
      <UserCredentials />
    </main>
  );
}
